# Changelog

## 0.1.0
- Inner I Gateway (FastAPI) with OPA policy enforcement
- Vetted tool registry + JSON schema validation
- Signed execution receipts + append-only audit hash chain
- Python SDK with `inneri.secure_agent_call()`
- Vault (dev) database secrets engine for JIT Postgres credentials
- JWT short-lived access tokens (Bearer) for gateway endpoints
